"""overlaybuilder - build ATAK-ready overlays for any US county from public GIS.

National baseline layers (roads/boundaries/buildings) work for every county by
FIPS. Local layers (parcels/addresses) come from a community catalog of small
declarative source files. Public, non-sensitive data only.
"""
__version__ = "0.2.0"
